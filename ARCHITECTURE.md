# 眼镜连锁-镜片调拨与库存预警系统

## 架构取舍与设计决策

### 1. 为什么选择 Tauri + SQLite

**取舍点：**
- ✅ **本地优先**：所有数据存储在本地 SQLite 数据库，完全支持离线使用
- ✅ **零依赖部署**：不需要服务器，单店可独立运行
- ✅ **轻量级**：相比 Electron，Tauri 包体小 10 倍以上
- ✅ **托盘提醒**：库存预警可通过系统托盘实时通知
- ❌ **多店同步暂缺**：当前版本为单店版本，后续可扩展为 P2P 同步
- ❌ **无云端备份**：依赖用户手动导出备份

### 2. 数据模型设计

**核心实体：**
```
验光单(optometry) → 加工记录(processing) → 调拨单(transfer)
                                    ↓
                               返修单(repair)
                                    ↓
                               退款单(refund)
```

**设计决策：**
- 所有单据通过 `optometry_id` 关联，形成完整链路
- 库存预警自动计算（quantity ≤ min_stock）
- 调拨单支持两种模式：关联验光单（客户订单驱动）/ 纯库存调拨（补货驱动）

### 3. 统一工作面设计

**设计理念：一单到底，不跳页**
- 左侧：验光单列表
- 右侧：选中客户的完整信息 + 全链路时间线
- 同屏展示：基本信息、验光处方、加工进度、调拨状态、返修/退款记录

**为什么这样做：**
- 避免在验光单、加工群、库存表三个地方切换
- 问题可追溯：任何环节出问题都能看到完整前因后果
- 追责清晰：每个操作都有时间和人员记录

### 4. 角色权限设计

| 角色 | 权限 | 典型操作 |
|------|------|----------|
| 店经理 | 全部权限 | 审批退款、创建调拨、标记丢失 |
| 验光师 | 只读 + 创建验光单 | 录入验光数据、查看进度 |
| 加工跟单 | 加工相关操作 | 开始/完成加工、签收调拨 |

**取舍点：**
- 前端权限控制（演示用），生产环境应在后端也做校验
- 角色切换位于左下角，方便演示不同视角

### 5. 样例数据说明

**正常流程样例（OPT003 王五）：**
```
验光完成 → 开始加工 → 加工完成 → 订单完成
```

**调拨丢失样例（TR002）：**
```
创建调拨 → 运输中 → 标记丢失
→ 触发追责流程
→ 可关联退款/返修
```

**返修流程样例（OPT004 赵六）：**
```
验光完成 → 加工完成 → 客户反馈 → 创建返修单
→ 可追踪：谁处理、原因是什么、费用多少
```

**退款流程样例（OPT005 孙七）：**
```
验光完成 → 客户投诉 → 申请退款 → 店经理审批
→ 前因后果完整记录，避免纠纷
```

## 后续扩展点

### Phase 1: 数据完整性
- [ ] 导入功能（Excel/CSV）
- [ ] 验光单录入表单
- [ ] 加工记录表单
- [ ] 返修/退款申请表单

### Phase 2: 多店协作
- [ ] P2P 数据同步（门店间直接同步）
- [ ] 冲突解决机制
- [ ] 总店数据汇总视图

### Phase 3: 智能化
- [ ] 基于历史数据的需求预测
- [ ] 自动补货建议
- [ ] 调拨路线优化
- [ ] 异常检测（异常丢失率、返修率）

### Phase 4: 集成
- [ ] 验光设备数据对接
- [ ] 快递物流 API 对接
- [ ] 财务系统对接
- [ ] 客户微信通知

## 运行方式

```bash
# 安装依赖
npm install

# 开发模式运行
npm run tauri dev

# 构建安装包
npm run tauri build
```

## 技术栈

- **前端**: React 18 + Zustand + Vite
- **后端**: Tauri (Rust) + SQLite
- **桌面能力**: 系统托盘、本地文件系统、通知
- **离线支持**: 100% 本地运行，无网络依赖
