# 游泳馆运营控制台 - 架构说明与扩展指南

## 一、实现取舍点 (Trade-offs)

### 1. 数据存储层选择：SQLite 本地数据库 vs 服务端数据库

**决策**：使用 `better-sqlite3` 作为本地嵌入式数据库

**理由**：
- 桌面端工具的核心价值在于离线能力，SQLite 完全在本地运行，无网络依赖
- 游泳馆单店场景数据量适中（万级记录），SQLite 性能绰绰有余
- 简化部署，不需要运维数据库服务
- 数据文件存储在系统用户目录，卸载应用不丢失数据

**代价**：
- 多终端数据同步需要额外开发（可通过云盘/数据库文件同步方案解决）
- 跨设备协作需要基于同步机制，而非实时共享

**未来扩展**：可增加可选的云端同步层，本地为主、云端为辅的混合架构

---

### 2. 技术栈选择：Electron + Vue3 而非原生开发

**决策**：使用 Electron 作为桌面框架，Vue3 作为 UI 框架

**理由**：
- 开发效率高，Web 生态丰富，组件库成熟
- 跨平台支持（Windows/macOS/Linux），一次开发多端运行
- Vue3 Composition API 适合复杂业务逻辑的组织
- 团队技能迁移成本低

**代价**：
- 内存占用比原生应用高（约 150-200MB）
- 启动速度略慢于原生应用
- 某些极底层的系统能力访问受限

**验证**：对于游泳馆运营工具这个场景，这些代价完全可接受。管理员不会频繁开关应用，内存占用也不是敏感指标。

---

### 3. 角色权限设计：三层角色 vs 细粒度 RBAC

**决策**：预设三种固定角色（馆长/教练主管/前台客服），而非完整的 RBAC 权限系统

**理由**：
- 游泳馆的角色分工相对固定，三种角色覆盖 90% 以上场景
- 固定角色的 UI/UX 设计更聚焦，每个角色看到的都是与自己相关的内容
- 避免过度设计，交付更快

**权限矩阵**：

| 功能模块 | 馆长 | 教练主管 | 前台客服 |
|---------|------|---------|---------|
| 控制台概览 | ✅ | ✅ | ✅ |
| 储物柜管理 | ✅ | ❌ | ✅ |
| 异常申诉 | ✅ | ✅ | ✅ |
| 申诉分配/升级 | ✅ | ❌ | ❌ |
| 课程表 | ✅ | ✅ | ❌ |
| 储值记录 | ✅ | ❌ | ✅ |
| 巡场照片 | ✅ | ✅ | ❌ |

**代价**：
- 如需增加新角色（如财务、维修人员）需要修改代码
- 不支持自定义角色和权限配置

**未来扩展**：如业务需要，可演进为基于角色的权限配置表，支持动态角色。

---

### 4. 界面风格：深色主题 vs 浅色主题

**决策**：默认深色主题

**理由**：
- 桌面工具通常长时间使用，深色主题减少视觉疲劳
- 控制台类工具（数据密集型）深色主题更易读
- 与 Electron 窗口标题栏风格统一（hiddenInset 模式下深色更协调）

**代价**：
- 部分用户可能偏好浅色主题
- 打印/截图场景浅色更友好

**未来扩展**：可增加主题切换功能，两套主题 CSS 变量一键切换。

---

### 5. 申诉流程设计：线性状态机 vs 自由流转

**决策**：申诉状态采用线性流转：待处理 → 调查中 → 已解决/已驳回/已升级

**理由**：
- 业务流程清晰，每个状态的责任人和下一步动作明确
- 时间线记录完整，追责可追溯
- 避免状态混乱，降低操作复杂度

**状态流转规则**：
```
pending (待处理)
    ↓ 开始调查
investigating (调查中)
    ├─→ resolved (已解决)
    ├─→ rejected (已驳回)
    └─→ escalated (已升级)
```

**代价**：
- 特殊场景下可能需要"回退"状态（如已解决后发现问题复发）
- 不够灵活，无法支持复杂的审批流

**未来扩展**：可在状态机中增加"重新打开"功能，或引入工作流引擎。

---

### 6. 储物柜分配：手动选择 vs 自动分配

**决策**：前台手动选择储物柜，而非系统自动分配

**理由**：
- 游泳馆场景下，用户常有偏好（如喜欢靠近淋浴区、喜欢下层等）
- 特殊需求（如带小孩需要相邻储物柜）需要人工判断
- 前台有现场认知，分配更合理

**代价**：
- 分配速度略慢于自动分配
- 可能出现分配不均（某些区域使用过度）

**未来扩展**：可增加"智能推荐"功能，基于历史使用数据推荐最优储物柜。

---

## 二、后续可扩展位置 (Extension Points)

### 1. 业务功能扩展

#### 1.1 会员管理模块
**当前状态**：仅有基础会员信息（储值记录中展示）
**扩展方向**：
- 完整的会员档案（照片、病史、偏好等）
- 会员卡类型（次卡、月卡、季卡、年卡）
- 会员到期提醒
- 消费画像和偏好分析

#### 1.2 预约系统
**当前状态**：无预约功能
**扩展方向**：
- 线上小程序预约课程/储物柜
- 预约到店核销
- 预约超时自动取消

#### 1.3 水质监测集成
**当前状态**：巡场照片手动记录水质问题
**扩展方向**：
- 接入水质监测设备 API
- 水质数据自动采集和预警
- 历史水质趋势分析

#### 1.4 门禁系统集成
**当前状态**：无
**扩展方向**：
- 会员卡刷卡入场
- 储物柜与门禁联动
- 在场人数实时统计

---

### 2. 技术架构扩展

#### 2.1 云端同步
**当前状态**：纯本地数据库
**扩展方向**：
- 可选的云端同步功能（业务数据备份）
- 多终端数据合并
- 冲突解决策略（基于时间戳 / 人工选择）

#### 2.2 多门店支持
**当前状态**：单门店设计
**扩展方向**：
- 门店维度数据隔离
- 馆长可查看多门店汇总数据
- 门店间资源调度（教练支援、会员转店）

#### 2.3 数据导出与报表
**当前状态**：无
**扩展方向**：
- 运营日报/周报/月报自动生成
- Excel/PDF 导出
- 自定义报表设计器

#### 2.4 消息通知
**当前状态**：无主动通知
**扩展方向**：
- 申诉状态变更通知（系统通知 + 微信推送）
- 课程开始提醒
- 储值余额不足提醒
- 系统公告

---

### 3. 用户体验优化

#### 3.1 快捷键覆盖
**当前状态**：仅支持菜单级快捷键
**扩展方向**：
- 全局快捷键（快速创建申诉、快速分配储物柜）
- 列表快捷键（上下选择、空格确认、删除）
- 可自定义快捷键映射

#### 3.2 拖拽交互
**当前状态**：无拖拽
**扩展方向**：
- 课程表拖拽调课
- 储物柜批量分配拖拽
- 申诉卡片拖拽更改状态

#### 3.3 数据可视化
**当前状态**：仅基础统计数字
**扩展方向**：
- 储物柜使用率热力图（按时间段）
- 申诉类型分布图
- 营收趋势图
- 课程出勤率分析

---

### 4. 系统集成能力

#### 4.1 API 开放层
**当前状态**：无对外 API
**扩展方向**：
- 本地 HTTP API 服务，供第三方系统调用
- Webhook 事件通知（申诉创建、课程变更）
- 标准 OAuth2 认证

#### 4.2 插件系统
**当前状态**：无
**扩展方向**：
- 插件市场，支持第三方开发功能扩展
- 插件沙箱环境，保证主程序稳定
- 插件生命周期管理（安装/启用/禁用/卸载）

---

## 三、核心数据模型扩展指南

### 增加新的申诉类型
1. 在 `src/types/index.ts` 的 `AppealType` 中增加新类型
2. 在 `APPEAL_TYPE_LABELS` 中增加显示名称
3. 在申诉创建表单中增加对应选项
4. 如需要特殊处理逻辑，在 `src/db/index.ts` 中扩展

### 增加新的业务实体
1. 在 `electron/main.ts` 的 `initDatabase()` 中创建新表
2. 在 `src/types/index.ts` 中定义 TypeScript 类型
3. 在 `src/db/index.ts` 中封装 CRUD 方法
4. 创建对应的视图组件（如有需要）
5. 在路由和菜单中配置入口

### 修改角色权限
1. 在 `src/router/index.ts` 中修改路由的 `meta.roles`
2. 在 `src/layout/MainLayout.vue` 中修改菜单项的 `roles`
3. 在组件内部使用 `userStore.hasRole()` 做细粒度控制

---

## 四、性能关注点

### 当前性能瓶颈（预估）
1. **申诉详情时间线**：单条申诉如果有上百条时间线记录，渲染可能卡顿
   - 优化：虚拟滚动 / 分页加载
2. **储物柜网格**：如单店超过 200 个储物柜，首次渲染可能慢
   - 优化：懒渲染 / 分页
3. **全文搜索**：当前无搜索功能，如增加全局搜索需要注意性能
   - 优化：SQLite FTS5 全文索引

### 数据库优化建议
- 已启用 WAL 模式，并发读写性能良好
- 重要查询字段已建索引（外键、状态、时间戳）
- 定期执行 `VACUUM` 整理数据库碎片
- 大数据量时考虑分表（如按月份分交易记录表）

---

## 五、安全考量

### 当前安全措施
1. **Context Isolation**：Electron 渲染进程与主进程隔离，避免 XSS 攻击
2. **无 Node Integration**：渲染进程无法直接访问 Node API，仅通过 preload 暴露有限接口
3. **SQL 参数绑定**：所有数据库操作使用参数化查询，防止 SQL 注入
4. **本地数据**：数据仅存储在用户本地，不上传任何服务器

### 需注意的风险
1. **数据库文件访问**：SQLite 数据库文件没有加密，物理接触设备可直接读取
   - 可扩展：使用 SQLCipher 加密数据库
2. **XSS 风险**：用户输入（申诉描述、备注等）直接渲染
   - 已做：Vue 默认会转义 HTML，除非使用 `v-html`
3. **无认证机制**：当前选择角色即可登录，无密码验证
   - 可扩展：增加登录密码 / 生物识别

---

*本文档记录架构决策，为后续维护和扩展提供参考。建议在做重大技术变更前先回顾此文档，理解当初的取舍理由。*
