# 地方剧院票务团单与退改审核系统 - 实现说明

## 一、核心架构

### 技术栈
- **前端**: Next.js 14 + TypeScript + Tailwind CSS + Zustand
- **后端**: Express + TypeScript + JWT认证
- **数据存储**: 内存数据库（可扩展至 PostgreSQL/MySQL）

---

## 二、实现取舍说明

### ✅ 已实现的核心功能

#### 1. 统一工作面设计
- **设计理念**: 单页整合场次、团单、退改审核三大模块，避免页面跳转
- **实现方式**: Tab 切换 + 场次筛选联动，一键查看特定演出的所有关联数据
- **业务价值**: 解决"在场次表、团单邮件、后台记录间来回切换"的痛点

#### 2. 三级角色权限体系
| 角色 | 用户名 | 权限范围 |
|------|--------|----------|
| 剧院经理 | manager | 场次管理、团单确认、退改终审、全功能访问 |
| 票务主管 | ticket | 团单管理、退改初审、收款登记 |
| 后台统筹 | backend | 场次排练安排、技术确认 |

#### 3. 场次变更自动联动
- **触发条件**: 演出时间变更或取消时
- **自动动作**: 
  - 关联团单状态自动标记为 "已变更"
  - 团单变更日志自动记录
  - 界面高亮提醒（黄色背景 + 文字提示）
- **解决痛点**: "临时改场次没人同步"

#### 4. 退改审批双审机制
- **流程**: 票务主管初审 → 剧院经理终审
- **状态流转**: PENDING → APPROVED_TICKET → APPROVED_MANAGER → COMPLETED
- **审批依据**: 系统自动关联原团单、原场次信息
- **解决痛点**: "退改审批缺依据"

#### 5. 费用结算联动
- 退改审批通过后自动更新团单结算金额
- 已收款项、退款金额、应收净额实时计算
- 结算状态自动流转（UNPAID → PARTIAL → SETTLED）
- **解决痛点**: "费用结算拖后腿"

#### 6. 批量复核面板
- 支持批量确认团单
- 支持批量审批退改申请（按角色区分可审批范围）
- 一键全选 + 批量执行
- **业务价值**: 提升高频重复操作效率

#### 7. 完整操作日志追踪
- 所有关键操作自动记录
- 包含操作人、时间、详情
- 支持按类型、用户筛选

### ⚠️ 当前实现的取舍与权衡

#### 1. 数据存储: 内存 vs 持久化
- **取舍**: 使用内存数据库而非真实数据库
- **原因**: 
  - 快速原型验证
  - 避免环境依赖问题
  - 重启即可重置为样例数据，方便测试
- **迁移成本**: 低 - 类型定义已完整，只需替换数据访问层

#### 2. 认证方式: JWT vs Session
- **取舍**: 使用 JWT 无状态认证
- **原因**:
  - 前后端分离架构友好
  - 水平扩展便利
- **注意**: 生产环境需考虑:
  - Token 刷新机制
  - 黑名单（主动登出）
  - HTTPS 强制启用

#### 3. UI 框架: 自研 vs 组件库
- **取舍**: 使用 Tailwind CSS 自定义样式，未引入 Ant Design 等重型组件库
- **原因**:
  - 保持代码轻量化
  - 减少打包体积
  - 样式完全可控
- **可扩展**: 可按需引入 antd / arco-design 等组件库

#### 4. 表单校验: 后端优先
- **取舍**: 核心校验逻辑放在后端，前端仅做基础校验
- **原因**:
  - 数据一致性由后端保证
  - 前端可逐步增强交互体验

---

## 三、内置业务样例说明

### 🎭 正常业务流（可直接演示）

#### 场景1: 新团单确认
- **团单**: GD202406004 - 和谐企业工会（80张，《茉莉花》音乐会）
- **状态**: PENDING
- **操作**: 使用 ticket/manager 账号，点击"确认"或批量操作

#### 场景2: 退改申请审批（两步流）
1. **票务初审**: TK202406001 - 星光社区（部分退票10张）
   - 状态: PENDING
   - 操作: 使用 ticket 账号审批通过
   
2. **经理终审**: TK202406002 - 市老年大学（改期）
   - 状态: APPROVED_TICKET（已通过票务初审）
   - 操作: 使用 manager 账号终审

### ⚠️ 问题场景（体现系统价值）

#### 场景3: 场次变更后的团单提醒
- **演出**: 《天鹅湖》芭蕾舞剧
- **事件**: 时间从 6月18日 → 6月20日（主演档期调整）
- **系统表现**:
  - 演出状态标记为 MODIFIED（黄色）
  - 关联的 100 张老年大学团单自动变黄高亮
  - 团单列表显示"场次已变更"提示
- **业务价值**: 避免"改期了没人知道"

#### 场景4: 改期申请的完整依据
- **申请**: TK202406002 - 市老年大学
- **关联数据**:
  - 原团单: GD202406003（100张，已付50%定金）
  - 原演出: 《天鹅湖》6月20日（已变更）
  - 拟改场次: 《茉莉花》6月25日
- **审批依据**: 系统内即可看到全部关联信息，无需翻找邮件

---

## 四、后续扩展点（按优先级）

### 🔴 P0 - 生产环境必备

1. **持久化数据库**
   - 替换内存存储为 PostgreSQL
   - 添加数据迁移工具 (Prisma / TypeORM)
   - 索引优化查询

2. **Token 安全增强**
   - Refresh Token 机制
   - Token 黑名单（Redis）
   - 密码强度策略

3. **生产环境部署**
   - PM2 进程管理
   - Nginx 反向代理
   - HTTPS 证书配置
   - 日志收集与监控

### 🟡 P1 - 业务增强

4. **邮件/消息通知**
   - 场次变更自动通知相关团单联系人
   - 审批进度通知
   - 到期提醒（演出前、付款前）

5. **导出功能**
   - 团单明细导出 Excel
   - 结算单导出 PDF
   - 操作日志审计导出

6. **高级筛选与搜索**
   - 多条件组合筛选
   - 全文搜索
   - 保存筛选条件

7. **数据统计报表**
   - 售票统计（按演出、按月）
   - 退改率分析
   - 收款进度追踪

### 🟢 P2 - 体验优化

8. **实时协作**
   - WebSocket 实时刷新
   - 多人编辑冲突提醒
   - 在线状态显示

9. **移动端适配**
   - 响应式布局优化
   - 原生 App 包装

10. **审批流程配置**
    - 可配置审批节点
    - 条件分支（金额阈值等）
    - 审批代理设置

---

## 五、启动说明

### 后端启动
```bash
cd backend
npm install
npm run dev
# 访问: http://localhost:3001
```

### 前端启动
```bash
cd frontend
npm install
npm run dev
# 访问: http://localhost:3000
```

### 测试账号
| 用户名 | 密码 | 角色 |
|--------|------|------|
| manager | 123456 | 剧院经理 |
| ticket | 123456 | 票务主管 |
| backend | 123456 | 后台统筹 |

---

## 六、项目结构

```
trae-test-1/
├── backend/                    # Express 后端
│   ├── src/
│   │   ├── types.ts           # 类型定义
│   │   ├── index.ts           # 服务入口
│   │   ├── data/
│   │   │   └── database.ts    # 内存数据库 + 样例数据
│   │   ├── middleware/
│   │   │   └── auth.ts        # JWT 认证中间件
│   │   └── routes/
│   │       ├── auth.ts        # 认证接口
│   │       ├── shows.ts       # 场次管理
│   │       ├── orders.ts      # 团单管理
│   │       ├── refunds.ts     # 退改审核
│   │       └── logs.ts        # 操作日志
│   └── package.json
│
└── frontend/                   # Next.js 前端
    ├── src/
    │   ├── pages/
    │   │   ├── _app.tsx
    │   │   ├── login.tsx      # 登录页
    │   │   └── index.tsx      # 主页面（统一工作面）
    │   ├── components/
    │   │   ├── Layout.tsx     # 布局组件
    │   │   └── UnifiedWorkspace.tsx  # 统一工作面核心
    │   ├── store/
    │   │   └── authStore.ts   # 认证状态管理
    │   ├── lib/
    │   │   └── api.ts         # API 客户端
    │   └── styles/
    │       └── globals.css    # 全局样式
    └── package.json
```
