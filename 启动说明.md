# 手作烘焙坊-原料盘点与损耗分析 - 系统启动说明

## 一、启动方式

### 1. 后端服务启动

```bash
# 进入后端目录
cd server

# 安装依赖（首次运行）
npm install

# 数据库初始化（首次运行）
npx prisma migrate dev
npx prisma generate
npm run prisma:seed

# 启动服务
npm run dev
```

后端服务地址：http://localhost:3001

### 2. 前端服务启动

```bash
# 进入前端目录
cd client

# 安装依赖（首次运行）
npm install

# 启动服务
npm run dev
```

前端服务地址：http://localhost:3000

## 二、角色切换方式

系统内置3个角色，在页面顶部右上角有角色切换下拉框：

| 角色 | Token | 说明 |
|------|-------|------|
| 门店主理人 | owner-token | 林店长 - 看全局进度、报表分析、审批退款 |
| 后厨负责人 | kitchen-token | 陈大厨 - 排产、生产操作、记录损耗 |
| 客服 | cs-token | 王客服 - 接单、改单、处理退款申请 |

**切换方式**：点击顶部导航栏右侧的角色选择框，选择不同角色即可切换权限和可见菜单。

## 三、角色权限说明

### 门店主理人 (OWNER)
- ✅ 仪表盘（全局统计）
- ✅ 订单管理（全部权限）
- ✅ 生产排期
- ✅ 原料管理
- ✅ 损耗分析
- ✅ 库存盘点
- ✅ 退款管理（审批权限）
- ✅ 审计日志

### 后厨负责人 (KITCHEN)
- ✅ 仪表盘（简化版）
- ✅ 生产排期（完整操作权限）
- ✅ 原料管理
- ✅ 损耗分析（记录权限）
- ✅ 库存盘点
- ❌ 订单管理（只读）
- ❌ 退款管理
- ❌ 审计日志

### 客服 (CUSTOMER_SERVICE)
- ✅ 仪表盘（简化版）
- ✅ 订单管理（接单、改单）
- ✅ 退款管理（申请权限）
- ❌ 生产排期（只读）
- ❌ 原料管理
- ❌ 损耗分析
- ❌ 库存盘点
- ❌ 审计日志

## 四、当前版本轻量化边界说明

### 1. 认证系统（轻量化）
- 当前使用固定 Token 认证，未实现真实登录
- Token 硬编码在中间件中
- 生产环境建议替换为 JWT + 数据库用户表

### 2. 幂等性（轻量化）
- 实现了基于 `X-Idempotency-Key` 的幂等中间件
- 当前仅在关键接口使用
- 未实现持久化幂等记录（内存存储）

### 3. 文件上传（未实现）
- 产品图片、凭证图片暂未实现文件上传功能
- 当前使用默认占位图

### 4. 实时通知（未实现）
- 状态变更通知、消息推送暂未实现
- 当前仅支持页面刷新查看

### 5. 分页查询（基础版）
- 列表查询暂未实现复杂分页
- 当前返回全部数据

### 6. 数据导出（未实现）
- 报表导出、Excel 导出暂未实现

### 7. 移动端适配（基础版）
- 未做深度移动端优化

## 五、核心链路演示数据说明

系统已预置完整演示数据：

### 订单数据（5个）：
- ORD202605300001 - 已完成（张女士）
- ORD202605310002 - 生产中（李先生，生日蛋糕）
- ORD202605310003 - 待确认（王小姐）
- ORD202605310004 - 已确认（赵先生，下午茶大单）
- ORD202605300005 - 已拒绝（孙女士，已退款）

### 生产数据（2个）：
- BAT202605300001 - 已完成
- BAT202605310001 - 生产中

### 损耗记录（2条）：
- 高筋面粉 0.5kg - 烤焦
- 鸡蛋 5个 - 打碎

### 退款记录（1条）：
- REF202605300001 - 已完成退款

### 审计日志（4条）：
- 订单状态流转全记录

## 六、接口调用说明

### 请求头要求：
```
Authorization: Bearer {token}
X-Idempotency-Key: {唯一键（可选，用于幂等）
```

### 关键接口列表：

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/orders | GET | 订单列表 |
| /api/orders | POST | 创建订单 |
| /api/orders/:id/confirm | POST | 确认订单 |
| /api/orders/:id/reject | POST | 驳回订单 |
| /api/orders/:id/start-production | POST | 开始生产 |
| /api/orders/:id/notes | POST | 添加备注 |
| /api/productions | GET | 生产列表 |
| /api/productions/:id/start | POST | 开始生产 |
| /api/productions/:id/complete | POST | 完成生产 |
| /api/productions/:id/rework | POST | 安排返工 |
| /api/waste | POST | 记录损耗 |
| /api/waste/analysis | GET | 损耗分析 |
| /api/refunds | GET | 退款列表 |
| /api/refunds | POST | 申请退款 |
| /api/refunds/:id/approve | POST | 批准退款 |
| /api/refunds/:id/reject | POST | 驳回退款 |
| /api/audit | GET | 审计日志 |
