# 胶片冲印 - 退款协商与赔付复核系统

## 技术栈
- 前端：Vue 3 + Vite + Pinia + TypeScript + Axios + Day.js
- 后端：NestJS + JWT + bcryptjs + 自定义 JSON 存储（零原生依赖，可无缝切换 PostgreSQL）
- 数据存储：JSON 文件（`backend/data/db.json`），演示环境轻量方案

---

## 一、启动方式

### 1. 启动后端服务
```bash
cd backend
pnpm install      # 首次需要安装依赖
pnpm start:dev    # 开发模式启动，运行在 http://localhost:3000
```

首次启动会自动生成演示数据（5个用户 + 8条胶卷 + 7条工单 + 完整状态流转和备注）。

### 2. 启动前端服务
```bash
cd frontend
pnpm install      # 首次需要安装依赖
pnpm dev          # 开发模式启动，运行在 http://localhost:5175
```

### 3. 访问系统
打开浏览器访问：http://localhost:5175

---

## 二、账号与角色切换

### 演示账号（密码统一：123456）

| 用户名   | 角色   | 姓名 | 权限范围 |
|---------|--------|------|---------|
| `owner` | 店主   | 李明 | 查看全部工单，审批赔付，批量操作 |
| `printer1` | 冲印师 | 张伟 | 查看分配给自己的冲印问题工单，处理返工 |
| `printer2` | 冲印师 | 王芳 | 同上 |
| `cs1`   | 客服   | 刘洋 | 登记问题，协商退款，查看分配给自己的工单 |
| `cs2`   | 客服   | 陈静 | 同上 |

### 角色切换方式

1. **登录页快速切换**：登录页下方有「快速切换角色（演示用）」区域，直接点击对应角色卡片即可一键登录，无需输入密码。

2. **系统内切换**：登录后，点击左侧边栏底部的用户信息区域（显示头像和姓名处），弹出角色切换菜单，点击不同角色即可切换。

---

## 三、核心页面说明

### 1. 批量复核面板（首页）- 高频处理场景
- **顶部统计卡片**：待处理、协商中、复核中、胶卷混号、版本错发、已完成 6项关键指标
- **多维度筛选**：状态、分类、问题类型、只看我的、搜索
- **看板式布局**：按状态分列展示工单卡片，支持多选操作
- **批量操作**：多选工单后可批量转入协商、提交复核、批准、标记完成、关闭
- **角色差异化**：
  - 店主：可见全部状态列，可批量批准赔付
  - 客服：仅可见待处理、协商中列，可转入协商、提交复核
  - 冲印师：仅可见与冲印相关的待处理、协商中列

### 2. 工单详情页 - 同一条工作面完成退款协商+赔付复核
- **左侧主区域**：
  - 工单基本信息（工单号、分类、问题类型、描述、客户诉求金额）
  - 关联胶卷信息（卷号、客户、品牌、ISO、**混号标记**、**交付版本**、混号说明）
  - 赔付方案（赔付类型、金额、客户承担、门店承担、原因）
  - 协商摘要（客服填写的协商结果）
  - 复核结论（店主填写的审批意见）
- **右侧时间线**：
  - 状态流转时间线（每次状态变更都记录时间、操作人、备注）
  - 备注记录（支持 4 种类型：内部、客户、协商、复核，可标记私有）
- **操作区**：根据角色和当前状态动态显示可用操作按钮
  - 客服：添加备注、转入协商、提交复核
  - 店主：批准赔付、拒绝赔付、标记完成、关闭工单
  - 冲印师：添加备注、标记返工完成

### 3. 胶卷管理
- 表格展示所有胶卷登记信息
- 可快速筛选混号胶卷、问题胶卷
- 支持查看关联工单

### 4. 综合查询
- 支持工单和胶卷的全文搜索
- 多条件组合筛选

---

## 四、演示数据典型场景

系统预置了 7 条工单，覆盖主要业务场景：

| 工单号 | 问题类型 | 客户 | 状态 | 说明 |
|--------|---------|------|------|------|
| WO202405200001 | 胶卷混号 | 张三 | 复核中 | 两卷柯达胶卷混装，客服已协商赔付方案，待店主审批 |
| WO202405200007 | 胶卷混号 | 李四 | 复核中 | 同上事件的关联工单，同一混号事故 |
| WO202405200002 | 版本错发 | 赵六 | 协商中 | 反转片 v2 版本发成 v1，正在协商 |
| WO202405190004 | 其他 | 吴九 | 已完成 | 客户取消订单，全额退款，完整审批链路 |
| WO202405220005 | 质量问题 | 王五 | 已完成 | 颜色偏色，免费返工，客户满意 |
| WO202405210003 | 质量问题 | 周八 | 待处理 | 冲洗划痕，冲扫队自检上报 |
| WO202405240006 | 交付延迟 | 郑十 | 待处理 | 冲扫机故障导致延期 |

**每条工单都包含**：完整的状态流转日志、多条不同类型的备注记录、明确的责任人、赔付方案（如适用）。

---

## 五、当前轻量化处理的边界（后续可扩展）

### 1. 数据存储层
- **现状**：使用 JSON 文件持久化（`backend/data/db.json`），完全不依赖数据库，零原生模块编译问题
- **边界**：无事务、无并发控制、无索引，适合演示和小规模使用
- **扩展**：数据模型完全兼容 TypeORM，切换到 PostgreSQL 只需配置数据库连接和恢复 Repository 调用

### 2. 用户与权限系统
- **现状**：3 种角色硬编码，权限控制基于角色判断，前端做视图过滤，后端做数据过滤
- **边界**：没有细粒度权限配置，没有角色管理界面，没有用户 CRUD

### 3. 文件与证据管理
- **现状**：证据图片仅存储 URL 路径，没有实际文件上传功能
- **边界**：没有文件上传、预览、下载功能

### 4. 通知与消息
- **现状**：状态变更、赔付审批等操作没有实时通知
- **边界**：没有站内信、短信、微信通知

### 5. 报表与数据分析
- **现状**：仅有简单的统计接口（按状态、问题类型统计）
- **边界**：没有多维度报表、趋势分析、导出功能

### 6. 工作流引擎
- **现状**：状态流转硬编码在 Service 层，状态机逻辑简单
- **边界**：没有可视化流程设计，没有复杂的条件分支和审批链

---

## 六、目录结构

```
trae-test-3/
├── backend/                    # NestJS 后端
│   ├── src/
│   │   ├── auth/              # 认证模块（登录、JWT、角色守卫）
│   │   ├── users/             # 用户模块
│   │   ├── film-rolls/        # 胶卷模块
│   │   ├── work-orders/       # 工单模块（核心）
│   │   ├── compensation/      # 赔付模块
│   │   ├── seed/              # 演示数据生成
│   │   ├── db/                # 自定义 JSON 存储服务
│   │   ├── app.module.ts
│   │   └── main.ts
│   ├── data/
│   │   └── db.json            # 数据存储文件（自动生成）
│   └── package.json
├── frontend/                   # Vue 3 前端
│   ├── src/
│   │   ├── views/             # 页面组件
│   │   │   ├── Login.vue      # 登录页（含快速角色切换）
│   │   │   ├── ReviewBoard.vue # 批量复核面板（核心）
│   │   │   ├── WorkOrderDetail.vue # 工单详情页
│   │   │   ├── FilmRolls.vue  # 胶卷管理
│   │   │   └── Search.vue     # 综合查询
│   │   ├── components/
│   │   │   └── Layout.vue     # 布局（侧边栏、角色切换）
│   │   ├── stores/            # Pinia 状态管理
│   │   ├── router/            # 路由配置
│   │   ├── api/               # API 封装
│   │   ├── utils/             # 工具常量
│   │   └── main.ts
│   └── package.json
└── QUICKSTART.md              # 本文档
```

---

## 七、API 清单

### 认证
- `POST /api/auth/login` - 登录

### 工单
- `GET /api/work-orders` - 获取工单列表（支持筛选）
- `GET /api/work-orders/stats` - 获取统计数据
- `GET /api/work-orders/:id` - 获取工单详情
- `POST /api/work-orders` - 创建工单
- `PATCH /api/work-orders/:id` - 更新工单（含状态流转）
- `POST /api/work-orders/batch` - 批量更新
- `GET /api/work-orders/:id/notes` - 获取备注列表
- `POST /api/work-orders/:id/notes` - 添加备注
- `GET /api/work-orders/:id/status-logs` - 获取状态流转日志

### 赔付
- `GET /api/compensation/work-order/:workOrderId` - 获取工单赔付记录
- `POST /api/compensation/work-order/:workOrderId` - 创建/更新赔付方案
- `PATCH /api/compensation/work-order/:workOrderId` - 审批赔付

### 胶卷
- `GET /api/film-rolls` - 获取胶卷列表
- `GET /api/film-rolls/:id` - 获取胶卷详情
- `PATCH /api/film-rolls/:id/status` - 更新胶卷状态

### 其他
- `POST /api/seed` - 重新生成演示数据

---

## 八、重置演示数据

如果需要重新生成演示数据：

```bash
# 方式 1：调用 API
curl -X POST http://localhost:3000/api/seed

# 方式 2：删除数据文件，重启后端
cd backend
rm -rf data
# 重启后端服务，会自动重新生成
```
