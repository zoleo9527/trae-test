import type { AvatarSize, BadgeColor, BadgeSize, BadgeVariant } from '../../types/index.js';
declare const _default: {
    wrapper: string;
    container: string;
    inner: string;
    base: string;
    before: string;
    after: string;
    active: string;
    inactive: string;
    label: string;
    icon: {
        base: string;
        active: string;
        inactive: string;
    };
    avatar: {
        base: string;
        size: AvatarSize;
    };
    badge: {
        base: string;
        color: BadgeColor;
        variant: BadgeVariant;
        size: BadgeSize;
    };
};
export default _default;
