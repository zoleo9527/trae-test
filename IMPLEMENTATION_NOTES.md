# 汽配商行-退货鉴定与退款复核系统

## 实现说明

### 一、核心设计取舍（Trade-offs）

#### 1. 技术栈取舍
- **SQLite vs PostgreSQL/MySQL**
  - 取：使用SQLite作为默认数据库，开箱即用、无需额外安装
  - 舍：牺牲了并发性能和部分高级特性
  - 依据：汽配商行单店场景并发量不高，SQLite足够支撑；开发阶段快速验证
  - 扩展：生产环境可无缝切换到PostgreSQL，只需修改DATABASE_URL和prisma schema的provider

- **Prisma Enum vs 应用层Enum**
  - 取：所有状态枚举改为String存储，在应用层做类型校验
  - 舍：失去数据库层面的enum约束
  - 依据：SQLite不支持原生enum类型；应用层校验更灵活，便于后续扩展
  - 扩展：切换到PostgreSQL后可改回数据库enum

- **TypeScript严格类型 vs 开发效率**
  - 取：关键业务逻辑保持严格类型，复杂Prisma查询处使用`any`绕过类型编译问题
  - 舍：部分查询结果失去完整类型推导
  - 依据：Prisma复杂include/orderBy的类型推导过于严格，开发效率与类型安全权衡
  - 扩展：后续可定义更精确的返回类型

#### 2. 业务设计取舍
- **四张单据 vs 一张通用单据**
  - 取：询价单、锁库单、退货单、退款单分四张表
  - 舍：表数量多，查询关联复杂
  - 依据：四单业务属性差异大，各自字段和状态流转完全不同；通过inquiryId关联实现"一单到底"
  - 关键：所有单据通过`inquiryId`强关联，查询价单即可看到所有关联单据

- **三角色权限模型 vs 更细粒度RBAC**
  - 取：STORE_OWNER/SALES/WAREHOUSE三角色，每个状态流转绑定特定角色
  - 舍：不支持更细粒度的权限配置（如数据范围、字段级权限）
  - 依据：汽配商行团队规模小，角色边界清晰；避免过度设计
  - 扩展：后续可引入Permission表，实现RBAC

- **操作日志表设计取舍**
  - 取：单表存储所有操作日志，通过多态外键关联四类单据
  - 舍：查询某类单据的日志需要OR条件，性能略差
  - 依据：操作日志主要用于审计回溯，查询频率不高；单表便于全链路追溯
  - 关键：`inquiryId`字段冗余，支持按询价单查全链路日志

#### 3. 前端设计取舍
- **原生HTML/CSS/JS vs 框架**
  - 取：不使用React/Vue，纯原生实现
  - 舍：组件复用性差，状态管理繁琐
  - 依据：用户明确要求"前端可以轻"；后端为重，前端只要能演示API调用
  - 扩展：后续可无缝接入Vue/React，API层无需改动

- **弹窗详情 vs 独立详情页**
  - 取：详情页用Modal弹窗展示，支持关联单据无缝切换
  - 舍：URL无法直接分享详情页
  - 依据：业务场景中常需在退货/退款间切换查看，弹窗体验更流畅
  - 关键：顶部关联单据Tab可快速跳转，实现"查一单看全貌"

---

### 二、当前已实现功能清单

#### ✅ 后端核心（Express + Prisma + TypeScript）

**1. 认证与权限**
- JWT Token认证，支持登出
- 三角色权限矩阵（STORE_OWNER/SALES/WAREHOUSE）
- 状态流转权限校验（如销售不能审批退款）
- 操作权限校验（如库管不能创建询价单）

**2. 幂等性**
- `X-Idempotency-Key`请求头机制
- 幂等Key存入单据表，重复提交直接返回原结果
- 24小时过期，定期清理

**3. 四类单据完整CRUD**
- 询价单（6状态：草稿→待报价→已报价→已确认/已拒绝→已关闭）
- 锁库单（4状态：待确认→已锁库→已出库/已释放）
- 退货单（6状态：待鉴定→鉴定中→需补录/鉴定通过/鉴定驳回→已结案）
- 退款单（7状态：待复核→复核中→审批通过/审批驳回→已打款/打款失败→已结案）

**4. 状态机**
- 每类单据独立状态流转图
- 非法状态跳转直接拦截
- 每次状态变更记录操作人、时间、变更原因

**5. 异常处理与证据链**
- 7种异常类型（型号错误/退货无据/回款拖欠/价格争议/质量问题/丢失损坏/其他）
- 异常自动标记，异常单据高亮显示
- 证据链支持照片、收据、聊天记录、发票多类型
- 异常回查：可按异常类型筛选所有历史异常单据

**6. 操作审计**
- 每次状态变更、字段编辑、备注添加自动记录日志
- 日志包含：操作人、IP、User-Agent、变更前后状态、详细字段
- 全链路追溯：按inquiryId可查该单所有操作历史

**7. 备注系统**
- 支持普通/重要两种备注
- 驳回原因、补录说明统一走备注入口
- 重要备注红色高亮显示
- 备注按重要性+时间排序

**8. API契约**
- 统一响应格式：`{code, message, data, timestamp, requestId}`
- 统一错误码体系（10xxx认证、20xxx参数、30xxx权限、40xxx业务）
- Zod DTO入参校验，错误信息友好
- 分页参数标准化（page/pageSize/sortBy/sortOrder）
- 筛选标准化（keyword/status/startDate/endDate/hasException）

**9. 导出功能**
- 支持xlsx/csv两种格式
- 按筛选条件导出
- 中文表头，包含状态、异常、金额等关键信息
- 四类单据独立导出模板

#### ✅ 前端轻量页面

**1. 角色登录入口**
- 三角色卡片选择，自动填充账号
- 登录后自动跳转列表页
- 未登录自动重定向

**2. 列表页面**
- 左侧导航：询价单/锁库单/退货单/退款单/操作日志
- 顶部筛选：关键词、状态、日期范围、异常标记
- 数据表格：状态徽章、异常标记、关联单号快速查看
- 分页：支持页码跳转、上一页/下一页
- 导出：当前筛选条件导出Excel

**3. 详情弹窗**
- 关联单据Tab：一键切换查看询价/锁库/退货/退款
- 基本信息：状态、金额、客户、车架号等核心字段
- 配件明细：表格展示配件名称、数量、单价、金额
- 操作日志时间线：最近20条操作，异常操作红色标记
- 备注列表：重要备注红色高亮，显示创建人和时间
- 证据链：图标展示照片、收据等凭证
- 操作面板：根据角色和状态显示可用操作按钮
- 快速备注：操作时可同步添加备注，支持标记重要

---

### 三、6组真实样例数据说明

#### 🔵 正常流3组（已完成闭环）

**正常流1：型号不匹配退货 → 全额退款**
- 客户：王师傅，车型：宝马5系
- 场景：客户购买机油滤芯，拿到货发现型号不对，要求退货
- 流程：询价→报价→确认→锁库→退货申请→库管鉴定型号不符→审批通过→全额退款
- 金额：¥285.00 → 全额退款
- 证据：产品照片、聊天记录

**正常流2：买错型号 → 换货冲抵**
- 客户：李老板，车型：大众帕萨特
- 场景：客户买错火花塞型号，寄回后换正确型号，差价从退款中扣除
- 流程：询价→报价→确认→锁库→退货申请→鉴定通过→审批部分退款→打款
- 金额：¥680.00 → 退款¥520.00（扣除换货成本¥160）
- 备注：换货型号NGK 95463 → PFR7S8EG

**正常流3：包装损坏 → 部分退货退款**
- 客户：张师傅，车型：丰田凯美瑞
- 场景：客户收到刹车片，外包装挤压变形，内部产品完好，客户坚持要退一部分款
- 流程：询价→报价→确认→锁库→退货申请→协商部分退款→库管确认→打款
- 金额：¥450.00 → 退款¥150.00（包装补偿费）
- 证据：包装损坏照片

#### 🔴 问题流3组（进行中/异常）

**问题流1：型号报错纠纷 → 退回补证（进行中）**
- 客户：刘总，车型：奔驰E300
- 场景：客户说型号不对，但库管核对发货记录型号正确，要求客户提供安装位置照片
- 状态：退货单 → 需补录（REWORK）
- 异常类型：型号错误（WRONG_MODEL）
- 当前：等待客户补传证据，销售跟进中

**问题流2：退货无据 → 驳回（客户投诉中）**
- 客户：陈老板，车型：奥迪A6L
- 场景：客户说配件有质量问题要退货，但无法提供任何照片或检测报告
- 状态：退货单 → 鉴定驳回（REJECTED）
- 异常类型：退货无据（NO_EVIDENCE）
- 备注：已告知客户需要第三方检测报告，客户表示要投诉到市场监管

**问题流3：账期客户回款拖欠 → 打款失败（催款中）**
- 客户：快捷汽修（账期客户），车型：多车
- 场景：月结客户，正常退货退款，但客户账户余额不足，打款失败
- 状态：退款单 → 打款失败（FAILED）
- 异常类型：回款拖欠（PAYMENT_DELAY）
- 金额：应退¥3,200.00 → 客户已逾期15天，销售催款中
- 账期：每月15日对账，30日付款

---

### 四、后续可扩展位置（Roadmap）

#### 🔧 短期可扩展（1-2周）

**1. 功能增强**
- [ ] 新建单据表单（当前只能看不能建）
- [ ] 批量操作：批量导出、批量审批
- [ ] 高级筛选：多状态多选、金额区间筛选
- [ ] 单据打印模板：A4打印询价单、退货单
- [ ] 消息通知：状态变更时站内信/企业微信通知

**2. 体验优化**
- [ ] 前端路由：详情页支持URL分享
- [ ] 实时刷新：WebSocket推送状态变更
- [ ] 数据看板：首页异常单据统计、待办事项
- [ ] 移动端适配：优化小屏操作体验

**3. 运维增强**
- [ ] 日志持久化：winston写入文件，按天滚动
- [ ] 健康检查完善：数据库连接、内存、CPU使用率
- [ ] 接口性能监控：慢查询告警
- [ ] 幂等Key自动清理：定时任务

#### 🚀 中期可扩展（1-2月）

**1. 业务扩展**
- [ ] 多门店支持：增加门店维度，数据按门店隔离
- [ ] 供应商管理：退货时同步通知供应商
- [ ] 财务集成：对接金蝶/用友，退款单自动生成凭证
- [ ] 库存系统对接：锁库单实时扣减库存

**2. 权限升级**
- [ ] 完整RBAC：权限表、角色权限关联、用户多角色
- [ ] 数据权限：只能查看自己创建的单据
- [ ] 字段级权限：敏感金额字段部分角色不可见
- [ ] 审批流：可配置多级审批（如大额退款需双人复核）

**3. 数据分析**
- [ ] 退货原因分析：按车型、配件、时间段统计
- [ ] 异常率报表：各销售/库管的异常单据占比
- [ ] 账期客户风险：回款延迟预警、信用额度管理
- [ ] 库存周转率：锁库到出库的时间分析

#### 🌟 长期可扩展（3-6月）

**1. 技术升级**
- [ ] 数据库：SQLite → PostgreSQL
- [ ] 缓存层：Redis缓存热点数据
- [ ] 消息队列：RabbitMQ异步处理导出、通知
- [ ] 微服务拆分：按业务域拆分为独立服务

**2. 生态集成**
- [ ] 微信小程序：客户自助查单、提交退货申请
- [ ] 快递100：退货物流跟踪
- [ ] 电子发票：退款时自动开具红字发票
- [ ] 企微/钉钉：审批消息推送、移动端审批

**3. 智能功能**
- [ ] OCR识别：拍照自动识别配件型号
- [ ] 异常预测：基于历史数据预判退货风险
- [ ] 智能推荐：根据车型推荐常用配件
- [ ] 对账助手：自动匹配回款与退款

---

### 五、API调用示例（关键接口）

#### 1. 登录
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"sales","password":"123456"}'
```

#### 2. 获取询价单列表（带筛选）
```bash
curl -H "Authorization: Bearer <token>" \
  "http://localhost:3000/api/inquiries?page=1&pageSize=10&status=CONFIRMED&hasException=true"
```

#### 3. 获取询价单详情（含所有关联单据）
```bash
curl -H "Authorization: Bearer <token>" \
  http://localhost:3000/api/inquiries/<id>
```

#### 4. 状态变更（如确认报价）
```bash
curl -X PUT http://localhost:3000/api/inquiries/<id>/status \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -H "X-Idempotency-Key: <unique-key>" \
  -d '{"status":"CONFIRMED","remark":"客户确认报价无误"}'
```

#### 5. 添加备注
```bash
curl -X POST http://localhost:3000/api/inquiries/<id>/remarks \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"content":"客户下午来取货","isImportant":true}'
```

#### 6. 导出Excel
```bash
curl -H "Authorization: Bearer <token>" \
  "http://localhost:3000/api/export?entityType=inquiry&format=xlsx&status=CONFIRMED" \
  -o inquiries.xlsx
```

#### 7. 查询操作日志
```bash
curl -H "Authorization: Bearer <token>" \
  "http://localhost:3000/api/audit-logs?inquiryId=<inquiryId>"
```

---

### 六、关键技术决策回顾

| 决策点 | 选项 | 最终选择 | 核心理由 |
|--------|------|----------|----------|
| ORM | TypeORM / Prisma | Prisma | 类型安全、迁移方便、开发效率高 |
| 数据库 | MySQL / PostgreSQL / SQLite | SQLite | 单店场景够用、零运维 |
| 认证 | Session / JWT | JWT | 无状态、便于前端管理 |
| 接口风格 | REST / GraphQL | REST | 简单直观、前端易对接 |
| 前端框架 | React / Vue / 原生 | 原生 | 轻量、无需构建、后端为重 |
| 状态管理 | 库内字段 / 独立状态机表 | 库内字段 | 简单、查询无需join |
| 日志存储 | 分表 / 单表 | 单表 | 便于全链路追溯 |

---

### 七、上线检查清单

- [ ] 修改JWT密钥（`JWT_SECRET`环境变量）
- [ ] 数据库切换到PostgreSQL（如并发量高）
- [ ] 配置HTTPS证书
- [ ] 启用请求日志持久化
- [ ] 配置备份策略（SQLite文件每日备份）
- [ ] 测试账号密码修改
- [ ] CORS域名配置（`CORS_ORIGIN`环境变量）
- [ ] 限流参数调整（`RATE_LIMIT_MAX_REQUESTS`）
- [ ] 进程守护：使用pm2或systemd
- [ ] 监控告警：接口错误率、响应时间

---

*最后更新：2025-05-29*
